import React from 'react';

const Noticias = () => {
    return (
        <div>
            <h1>Contact Page</h1>
        </div>
    );
};

export default Noticias;  // Asegúrate de que esté exportando el componente
